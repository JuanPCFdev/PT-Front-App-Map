package com.jpdev.ptapplicationmap.presentation.ui.theme

import androidx.compose.ui.graphics.Color

//App Colores
val DarkBackground = Color(0xFF030712)    // Fondo principal oscuro
val DarkSurface    = Color(0xFF111828)    // Superficies (tarjetas, AppBar, etc.)
val OnBackground   = Color(0xFFFFFFFF)    // Texto principal sobre fondo oscuro

val Primary        = Color(0xFF2563ea)    // Botón “Iniciar Sesión” (turquesa)
val PrimaryVariant = Color(0xFF018D79)    // Un turquesa más oscuro
val OnPrimary      = Color(0xFFFFFFFF)    // Texto sobre botón primario (blanco)

val Secondary      = Color(0xFF00172C)    // Puedes usar un secundario más suave si quieres
val OnSecondary    = Color(0xFF000000)    // Texto sobre secundario (oscuro)

// Colores de error (estándar Material)
val ErrorColor     = Color(0xFFCF6679)
val OnError        = Color(0xFF000000)
